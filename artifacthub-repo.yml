# Artifact Hub repository metadata. This file proves to Artifact Hub that the
# people listed below control this repository, which is what lets the listing
# claim ownership (verified publisher, ownership transfer, package deletion)
# rather than being an anonymous mirror of the chart.
#
# The repositoryID is issued by Artifact Hub when the repository is registered
# in its UI; it is not a secret and it is not derived from anything in this
# repository, so it is checked in verbatim and must never be regenerated by
# hand.
#
# Where Artifact Hub reads this from depends on how the repository is served.
# The chart ships as an OCI artifact (oci://ghcr.io/kuberecord/charts/kuberecord
# — see the Makefile's CHART_OCI_NAMESPACE and .github/workflows/release.yml),
# and for an OCI-based Helm repository Artifact Hub does not read this path: the
# file has to be pushed to the registry as its own artifact alongside the chart.
# It is kept here as the single source of truth that such a push publishes.
repositoryID: e05db200-e54b-40b8-802a-cc0b7862fd43
owners:
  - name: Yelyzaveta Zhyrova
    email: zhyrova.yelyzaveta@gmail.com
